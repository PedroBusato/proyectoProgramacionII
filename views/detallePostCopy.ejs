<!DOCTYPE html>
<html lang="en">

<%- include("./partials/head") %>

<body>
    <%- include("./partials/header") %>
    <main>
        <div class="uk-section uk-section-xsmall">
            <div class="uk-container uk-container-large uk-flex uk-flex-middle uk-flex-center">
                <section class="uk-flex uk-width-2-3 detalle-container">
                    <article class="uk-width-2-3 uk-flex uk-flex-center uk-flex-middle detalle-post">
                        <img src="<%= post.image %>" alt="Imagen del post" class="imagen-post ">
                    </article>
                    <article class="uk-width-1-3 uk-flex uk-flex-column comentarios-detalle">
                        <div class="info-post uk-flex uk-flex-middle">
                            <img src="<%= post.user.profilePic%>" alt="Foto del usuario">
                            <h3><a href="/profile/userDetail/<%= post.user.userName %>"><%= post.user.userName %></a></h3>
                        </div>
                        <div class="lista-comentarios" >
                            <% for( let i = 0; i < post.comments.length; i++ ) { %>
                                <div class="comentario uk-flex uk-flex-middle uk-margin-small-top">
                                    <div class="uk-width-1-3 uk-flex uk-flex-center">
                                        <div class="uk-width-2-4 uk-flex uk-flex-center">
                                            <img src="<%= post.comments[i].user.profilePic %>" alt="Foto de usuario">
                                        </div>
                                    </div>
                                    <h6 class="uk-margin-remove uk-width-2-4"><a href="/profile/userDetail/<%= post.comments[i].user.userName%>"> <%= post.comments[i].user.userName %></a> <%= post.comments[i].commentText %></h6>
                                    <a href="" uk-icon="icon:heart; ratio: 0.8" class="uk-width-1-4 uk-text-right icono-meGusta"></a>
                                </div>
                            <% } %>                           
                        </div>
                        
                        <div class="agregar-comentario uk-flex uk-flex-middle">
                            <form method="POST" class="uk-text-center">
                              <input name="commentText" type="text" class="uk-input" placeholder="Agregar comentario"> 
                              <button class="uk-button uk-button-primary uk-margin-small-top" type="submit">Comentar</button>
                            </form>
                        </div>
                    </article>
                </section>
            </div>
        </div>
    </main>

    <%- include("./partials/footer")%> 

</body>

</html>